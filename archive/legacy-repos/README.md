# Legacy Repositories Archive

This folder is for local archived repository clones that are no longer the active source of truth.

## `indot-website/`

Archived from `D:\My Projects\InDOT-website`.

This is an older website-only clone of `s2hublab/InDoT-map-project`. It is kept locally as a reference copy. The active redesigned website source lives in `website/` at the root of this repository.

The `indot-website/` clone is intentionally ignored by the parent repository because it contains its own `.git` directory.
