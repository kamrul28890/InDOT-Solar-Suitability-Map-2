import { PlaceholderPage } from '../PlaceholderPage';

export default function FaqPage() {
  return (
    <PlaceholderPage
      eyebrow="FAQ"
      title="Frequently asked questions"
      description="FAQ content is optional in the spec and may be folded into About if the final site does not need a separate page."
    />
  );
}
