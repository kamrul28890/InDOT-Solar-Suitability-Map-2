# Static Map App

Static deployable map app used by the editor preview and final export ZIP.

This will be adapted from the Phase 1 React/Leaflet map so it can load data from relative static paths instead of the live FastAPI routes.

